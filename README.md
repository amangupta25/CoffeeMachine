# Coffee Machine
Implements a working Coffee Machine, which can serve from multiple outlets in parallel, using multi-threading


## To build:
```mvn clean install```
## To run all test cases:
```mvn test```
<br><br> _Note:_ _All the test cases inputs are present as json files at path:  `./src/main/resources/{filename}.json`_
